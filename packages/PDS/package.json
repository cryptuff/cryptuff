{
  "name": "@cryptuff/pds",
  "version": "0.0.1",
  "description": "Public Data Service for crypto markets",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "build:watch": "rimraf ./dist && tsc --watch",
    "dev": "tsc --watch & nodemon dist",
    "test": "jest"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@cryptuff/core": "latest"
  },
  "devDependencies": {}
}
