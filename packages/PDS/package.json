{
  "name": "@cryptuff/pds",
  "version": "0.0.1",
  "description": "Public Data Service for crypto markets",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "build:watch": "rimraf ./dist && tsc --watch",
    "tscbuild": "tsc --build --watch",
    "dev": "tsc --watch & nodemon dist",
    "test": "jest"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "autobahn": "^18.10.2",
    "tslib": "^1.9.3"
  },
  "devDependencies": {
    "@types/autobahn": "^0.9.41",
    "@types/node": "^10.12.18"
  }
}
